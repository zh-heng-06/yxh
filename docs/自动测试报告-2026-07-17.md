# 掌柜台自动测试报告（2026-07-17）

## 结论

- 独立测试数据库全链路：**33 项通过，0 项软件断言失败**。
- 正式数据库：`pragma quick_check = ok`，测试前后仍为 1 台设备，未写入测试数据。
- 正式局域网地址：`http://192.168.0.120:4180/` 返回 200，电脑地址和所有前端资源正常。
- 打印机：驱动和 `USB001` 端口存在，但 Windows 底层状态为 `WorkOffline=True`；实体打印未出纸，因此实体打印项判定为**未通过/待恢复在线后复测**。

## 已通过范围

1. 服务启动、SQLite 自检、首次设置、老板登录。
2. 爱思助手真实截图本地 OCR：识别 `iPhone 16 Pro Max` 和 IMEI `357507790090396`。
3. 15 位 IMEI 校验、低于成本二次确认、重复 IMEI 保护。
4. Apple、华为、小米三台设备入库及唯一库存编号。
5. 详情编辑、成本字段、原始截图留档、瑕疵照片保存和读取。
6. 40×30 标签预览生成，二维码可反向识别为正确库存编号。
7. 预订、取消预订、送修、维修完成。
8. 盘点重复扫描、错区域、漏扫和完成盘点。
9. 成交、重复出库保护、销售退货、重新入库后再次销售。
10. 退货冲减后：今日售出 1 台、净销售额 ¥7,100、净利润 ¥700。
11. 库存/销售 CSV 导出、本地文字入库解析、定价建议、销售文案、经营总结。
12. 店员看不到成本和完整 IMEI，也不能进入老板账号管理。
13. 手工备份、CSV 导入、恢复备份、关键操作日志。
14. 浏览器主页面、设置、报表和打印按钮实际点击。

## 本次测试发现并已修复

1. 同一秒内创建备份并恢复时，安全备份可能与目标备份重名。现已使用微秒级唯一文件名。
2. 报表接口净利润正确，但页面原来展示退货前毛利。现已改为显示“净利润 ¥700”。
3. `Get-Printer` 显示 Normal 时可能同时处于 Windows 脱机状态。现已联合检测 `WorkOffline`，并在离线时阻止打印，避免任务滞留和网页误报已打印。

## 实体打印复测条件

先让 Windows 的 `WorkOffline` 变为 `False`，再打印一张测试标签。需要实物确认：只出一张、40×30 横向、无边框、六行不越界、二维码可扫。当前测试任务已经从打印队列清除，不会在恢复连接后突然补打。

自动回归脚本：`tests/integration_smoke.py`。
